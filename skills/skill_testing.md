# Skill：测试（v0.2）

## 最低要求
- L0：lint/build 通过 + 手工验收用例
- L1：新增/修改逻辑必须有单测（service 层优先）
- L2/L3：必须补集成/契约测试，覆盖兼容场景

## 必测清单（L2+）
- feature flag 开/关两条链路都通
- 回滚模拟：旧版本逻辑读新数据不崩
- 基本性能：关键接口延迟不显著劣化（至少给对比）
- 至少 1 条兼容性测试必须写进 MR 的 Test Evidence（怎么测 + 结果）
