# Skill：前端领域准则（v0.2）

## 适用范围
只约束 FE 实现细节（组件、状态管理、设计系统、性能预算）。
不得与 CLAUDE.md 的"全局宪法"冲突（兼容/契约/安全优先）。

## FE 交付必须项
- 与 API 契约一致（skill_api_contract）
- 容忍未知字段/新增字段（兜底 UI）
- 若 BE 使用开关：FE 必须配合（开关开/关体验都完整）

## 常见冲突处理
- 字段重命名：禁止。改为新增字段 + 兼容旧字段 + 废弃周期
- 大重构：拆分多 MR，小步灰度
