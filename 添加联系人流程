flowchart TD
  A[添加入口（+）<br/>入口列表：添加联系人 / 创建群聊 / 创建频道 / 创建社区 / 扫一扫] --> B[点击：添加联系人]
  B --> C[添加联系人页<br/>- 输入：对方手机号 或 即我号<br/>- 输入：名称备注（可选）<br/>- 开关：同步到手机通讯录<br/>- 按钮：确认添加<br/>- 其他方式：扫描二维码 / 从通讯录添加 / AI匹配]

  %% 入口分支
  C -->|手动输入手机号/即我号| D[输入标识（手机号/即我号）]
  C -->|扫描二维码| Q1[点击：扫描二维码]
  C -->|从通讯录添加| P1[点击：从通讯录添加]
  C -->|AI 匹配| I1[点击：AI 匹配]

  %% 手动输入主链路
  D --> E{输入合法？<br/>- 手机号格式正确<br/>- 即我号：字母开头<br/>- 不能是自己的号码/即我号}
  E -->|否| E1[提示错误<br/>并聚焦输入框] --> D
  E -->|是| F{命中本地通讯录？<br/>（你原型红字：命中通讯录需自动筛选）}
  F -->|是| F1[提示通讯录候选/自动带入备注（可修改）] --> G[查询服务器：是否存在该用户]
  F -->|否| G[查询服务器：是否存在该用户]

  G --> H{对方已注册即我？}
  H -->|是| H1[展示“已注册”样式<br/>头像/昵称/最近登录状态] --> R[输入名称备注（可选）]
  H -->|否| H2[展示“未注册”样式<br/>提示：可邀请（短信/分享）<br/>允许继续添加为“待注册联系人”】【并支持留言】] --> R

  R --> S{同步到手机通讯录开关=开？}
  S -->|开| S1{通讯录权限通过？}
  S1 -->|否| S2[弹系统授权/引导去设置<br/>（不阻断继续添加）] --> U
  S1 -->|是| S3[写入/更新本地通讯录] --> U
  S -->|关| U[点击：确认添加]

  U --> V{该联系人已存在？}
  V -->|是| V1[提示：已添加<br/>可更新备注 / 直接聊天] --> W[跳转：私聊窗口<br/>（自动打开与该联系人的会话）]
  V -->|否| V2[创建/更新联系人记录<br/>Contact/Remark/SyncFlag/RegStatus] --> W

  %% 私聊窗口分支（你原型右侧红字）
  W --> X{私聊页：对方已注册即我？}
  X -->|是| X1[正常私聊<br/>可直接发送消息<br/>显示在线/最近登录] --> Z((结束：已进入私聊并可发起对话))
  X -->|否| X2[未注册私聊（留言态）<br/>顶部提示“对方未注册”<br/>输入框文案：留言<br/>消息状态：待送达（仅你可见）<br/>CTA：邀请注册<br/>对方注册后：自动送达并转正常私聊] --> Z

  %% 扫码分支
  Q1 --> Q2{相机权限通过？}
  Q2 -->|否| Q3[提示授权/引导去设置] --> C
  Q2 -->|是| Q4[扫描二维码] --> Q5{识别成功？}
  Q5 -->|否| Q4
  Q5 -->|是| Q6{二维码类型？}
  Q6 -->|个人名片/即我号| D
  Q6 -->|群聊/频道/社区| Q7[跳转对应加入流程<br/>（本图不展开）]

  %% 通讯录分支
  P1 --> P2{通讯录权限通过？}
  P2 -->|否| P3[提示授权/引导去设置] --> C
  P2 -->|是| P4[选择联系人] --> P5{多个手机号？}
  P5 -->|是| P6[选择一个号码] --> D
  P5 -->|否| D

  %% AI匹配分支
  I1 --> I2[展示推荐列表/搜索] --> I3[选择对象（得到标识）] --> D
