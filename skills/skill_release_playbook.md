# Skill：发布与灰度（v0.2）

## L1 默认流程
staging 验证 → 合并 → 小流量灰度 → 全量

## L2/L3 必须项
- 开关（feature flag/version gate）+ 默认值策略（必须安全默认）
- 观测指标：错误率、延迟、队列堆积/重试量、DB 慢查询
- 回滚条件（阈值）+ 回滚步骤（可执行）

## 推荐灰度维度
- user_id 哈希百分比
- 客户端版本范围
- 白名单/租户
