# Spec Packet（v0.2）
> 只写"用户可见 + 验收 + 约束"。技术黑盒（接口/DB/模型/存储选型）由 Tech Brief/Decision Pack 补齐。

## 1) 目标 & 非目标
- 目标：
- 非目标（明确不做）：

## 2) 用户路径（1-3 条）
- Path A：
- Path B：

## 3) 页面/交互/状态（若涉及 UI）
- 状态列表（加载中/空/成功/失败/无权限/弱网等）：
- 核心交互（点击/输入/撤销/重试/引导等）：

## 4) 输入/输出（系统能力 I/O）
- 输入：用户/系统触发了什么（示例：提交文本/上传音频/选择主题）
- 输出：用户看到什么结果（示例：摘要/列表/标签/提示/错误）

## 5) 关键约束（必须）
- 兼容性：老客户端/老数据/回滚后的行为要求
- 隐私与合规：数据是否允许出网？是否包含敏感信息？
- 延迟预算：用户操作到看到结果可接受的时间（给个区间也行）
- 成本预算：日调用量/QPS 粗估、是否能用 GPU、是否必须自部署
- 可靠性：允许降级吗？降级时用户看到什么？

## 6) 验收用例（Given-When-Then，至少 5 条）
1)
2)
3)
4)
5)

## 7) 上线与回滚（业务视角）
- 是否需要开关（是/否，原因）：
- 灰度方式（白名单/百分比/版本范围）：
- 上线后重点观测（错误率/延迟/投诉等）：
- 触发回滚的条件：
